<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

   <!-- 회원가입(회원추가) -->
   <insert id="memberInsert" parameterType="mbdto">
      insert into member(member_num,id,password,name,gender,birth,phone,email,joinday,grade) values (
      memberseq.nextval,#{id},#{password},#{name},#{gender},#{birth},#{phone},#{email},sysdate,'c')
   </insert>
   
   <!-- 아이디 확인 -->
<select id="idCheck" parameterType="String" resultType="int">
	select id from member where id = #{id}
</select>
   
</mapper>